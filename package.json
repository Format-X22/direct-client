{
  "name": "direct-client",
  "version": "0.0.1",
  "description": "",
  "main": "./src/index.js",
  "scripts": {
    "start": "electron .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Format-X22",
  "license": "GPL-3.0",
  "prettier": {
    "singleQuote": true,
    "tabWidth": 4,
    "semi": true,
    "trailingComma": "es5",
    "printWidth": 100,
    "overrides": [
      {
        "files": [
          "*.json",
          "*.yml"
        ],
        "options": {
          "tabWidth": 2
        }
      }
    ]
  },
  "dependencies": {
    "pouchdb": "^7.0.0"
  },
  "devDependencies": {
    "electron": "^4.1.4"
  }
}
