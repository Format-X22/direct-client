{
  "name": "direct-client",
  "version": "0.0.1",
  "description": "",
  "main": "./src/index.js",
  "scripts": {
    "start": "electron .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Format-X22",
  "license": "GPL-3.0",
  "prettier": {
    "singleQuote": true,
    "tabWidth": 4,
    "semi": true,
    "trailingComma": "es5",
    "printWidth": 100,
    "overrides": [
      {
        "files": [
          "*.json",
          "*.yml"
        ],
        "options": {
          "tabWidth": 2
        }
      }
    ]
  },
  "dependencies": {
    "electron-store": "^3.2.0",
    "gls-core-service": "^3.18.0",
    "keypair": "^1.0.1"
  },
  "devDependencies": {
    "electron": "^4.1.4"
  }
}
