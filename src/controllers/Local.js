const core = require('gls-core-service');
const BasicController = core.controllers.Basic;

class Local extends BasicController {
    async createAccount(event, args) {
        // TODO -
    }
}

module.exports = Local;
